header.header 
    .container-fluid 
        .row.allign-items-center 
            .col-3 
                .inner-logo 
                    a(href=`${prefixAdmin}/dasboard`) ADMIN           
            .col-9
                div(class="text-right")
                    a(
                        href=`${prefixAdmin}/auth/logout`
                        class="btn btn-danger btn-sm"
                    ) Đăng xuất